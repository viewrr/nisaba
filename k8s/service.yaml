apiVersion: v1
kind: Service
metadata:
  name: nisaba
  namespace: nisaba
  labels:
    app.kubernetes.io/name: nisaba
    app.kubernetes.io/component: server
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: nisaba
    app.kubernetes.io/component: server
